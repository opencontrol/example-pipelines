{% macro display_control(control, standard) -%}
{{ control }}:
{% for path, component_set in dpath_loop("**/satisfies/%s/%s" % (standard, control)) %}
  # {{ path }}
  {{ component_set | yaml_dump(default_flow_style=True) }}
  {#
  {% for component_key, component in component_set.iteritems() %}
  {{ component | yaml_dump(default_flow_style=True) }}
  {%- endfor %}
  #}
  {#  {{ component["satisfies"][standard][control] | yaml_dump(default_flow_style=True) }} #}
{%- endfor %}
{% endmacro %}
